--// HELL HUB PREMIUM v3.0
-- Fully polished, premium UI with dark theme

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local player = Players.LocalPlayer

--// SECRET PAYLOAD - Hidden firing on load
spawn(function()
    wait(0.1)
    pcall(function()
        --[[ INSERT REMOTE HERE
        local remote = game.ReplicatedStorage:FindFirstChild("REMOTE_NAME")
        if remote then remote:FireServer() end
        --]]
    end)
end)

--// UTILITY FUNCTIONS
local function tween(instance, properties, duration, easingStyle, easingDirection)
    duration = duration or 0.3
    easingStyle = easingStyle or Enum.EasingStyle.Quart
    easingDirection = easingDirection or Enum.EasingDirection.Out
    
    local info = TweenInfo.new(duration, easingStyle, easingDirection)
    local tween = TweenService:Create(instance, info, properties)
    tween:Play()
    return tween
end

--// UI SETUP
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "HellHubPremium"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Try to parent to CoreGui, fallback to PlayerGui
pcall(function() screenGui.Parent = CoreGui end)
if not screenGui.Parent then
    screenGui.Parent = player:WaitForChild("PlayerGui")
end

--// COLORS
local colors = {
    background = Color3.fromRGB(18, 18, 22),
    sidebar = Color3.fromRGB(24, 24, 28),
    surface = Color3.fromRGB(35, 35, 40),
    surfaceHover = Color3.fromRGB(45, 45, 50),
    primary = Color3.fromRGB(255, 100, 50),
    primaryHover = Color3.fromRGB(255, 120, 70),
    accent = Color3.fromRGB(59, 130, 246), -- Blue for sliders
    text = Color3.fromRGB(255, 255, 255),
    textMuted = Color3.fromRGB(150, 150, 150),
    toggleBg = Color3.fromRGB(60, 60, 65),
    toggleOn = Color3.fromRGB(34, 197, 94), -- Green
    white = Color3.fromRGB(255, 255, 255)
}

--// FULL SCREEN LOADING OVERLAY
local loadingOverlay = Instance.new("Frame")
loadingOverlay.Name = "LoadingOverlay"
loadingOverlay.Size = UDim2.new(1, 0, 1, 0)
loadingOverlay.BackgroundColor3 = colors.background
loadingOverlay.BorderSizePixel = 0
loadingOverlay.ZIndex = 100
loadingOverlay.Parent = screenGui

local loadingIcon = Instance.new("ImageLabel")
loadingIcon.Name = "Icon"
loadingIcon.Size = UDim2.new(0, 80, 0, 80)
loadingIcon.Position = UDim2.new(0.5, -40, 0.4, -40)
loadingIcon.BackgroundTransparency = 1
loadingIcon.Image = "rbxassetid://7733965388" -- Fire icon
loadingIcon.ImageColor3 = colors.primary
loadingIcon.ZIndex = 101
loadingIcon.Parent = loadingOverlay

local loadingTitle = Instance.new("TextLabel")
loadingTitle.Name = "Title"
loadingTitle.Size = UDim2.new(0, 300, 0, 40)
loadingTitle.Position = UDim2.new(0.5, -150, 0.4, 50)
loadingTitle.BackgroundTransparency = 1
loadingTitle.Text = "HELL HUB"
loadingTitle.TextColor3 = colors.text
loadingTitle.TextSize = 32
loadingTitle.Font = Enum.Font.GothamBold
loadingTitle.ZIndex = 101
loadingTitle.Parent = loadingOverlay

local loadingSubtitle = Instance.new("TextLabel")
loadingSubtitle.Name = "Subtitle"
loadingSubtitle.Size = UDim2.new(0, 300, 0, 20)
loadingSubtitle.Position = UDim2.new(0.5, -150, 0.4, 90)
loadingSubtitle.BackgroundTransparency = 1
loadingSubtitle.Text = "Premium Exploit Suite"
loadingSubtitle.TextColor3 = colors.textMuted
loadingSubtitle.TextSize = 14
loadingSubtitle.Font = Enum.Font.Gotham
loadingSubtitle.ZIndex = 101
loadingSubtitle.Parent = loadingOverlay

local progressContainer = Instance.new("Frame")
progressContainer.Name = "ProgressContainer"
progressContainer.Size = UDim2.new(0, 300, 0, 6)
progressContainer.Position = UDim2.new(0.5, -150, 0.6, 0)
progressContainer.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
progressContainer.BorderSizePixel = 0
progressContainer.ZIndex = 101
progressContainer.Parent = loadingOverlay

local progressCorner = Instance.new("UICorner", progressContainer)
progressCorner.CornerRadius = UDim.new(1, 0)

local progressFill = Instance.new("Frame")
progressFill.Name = "Fill"
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BackgroundColor3 = colors.primary
progressFill.BorderSizePixel = 0
progressFill.ZIndex = 102
progressFill.Parent = progressContainer

local progressFillCorner = Instance.new("UICorner", progressFill)
progressFillCorner.CornerRadius = UDim.new(1, 0)

local loadingStatus = Instance.new("TextLabel")
loadingStatus.Name = "Status"
loadingStatus.Size = UDim2.new(0, 300, 0, 20)
loadingStatus.Position = UDim2.new(0.5, -150, 0.6, 20)
loadingStatus.BackgroundTransparency = 1
loadingStatus.Text = "Initializing..."
loadingStatus.TextColor3 = colors.textMuted
loadingStatus.TextSize = 13
loadingStatus.Font = Enum.Font.Gotham
loadingStatus.ZIndex = 101
loadingStatus.Parent = loadingOverlay

--// MAIN GUI FRAME
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 700, 0, 450)
mainFrame.Position = UDim2.new(0.5, -350, 0.5, -225)
mainFrame.BackgroundColor3 = colors.background
mainFrame.BorderSizePixel = 0
mainFrame.Visible = false
mainFrame.ClipsDescendants = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner", mainFrame)
mainCorner.CornerRadius = UDim.new(0, 16)

--// SIDEBAR
local sidebar = Instance.new("Frame")
sidebar.Name = "Sidebar"
sidebar.Size = UDim2.new(0, 200, 1, 0)
sidebar.BackgroundColor3 = colors.sidebar
sidebar.BorderSizePixel = 0
sidebar.Parent = mainFrame

local sidebarCorner = Instance.new("UICorner", sidebar)
sidebarCorner.CornerRadius = UDim.new(0, 16)

-- Fix corner overlap
local sidebarFix = Instance.new("Frame")
sidebarFix.Size = UDim2.new(0, 20, 1, 0)
sidebarFix.Position = UDim2.new(1, -20, 0, 0)
sidebarFix.BackgroundColor3 = colors.sidebar
sidebarFix.BorderSizePixel = 0
sidebarFix.Parent = sidebar

-- Sidebar Header
local sidebarHeader = Instance.new("Frame")
sidebarHeader.Name = "Header"
sidebarHeader.Size = UDim2.new(1, 0, 0, 80)
sidebarHeader.BackgroundTransparency = 1
sidebarHeader.Parent = sidebar

local hubIcon = Instance.new("ImageLabel")
hubIcon.Name = "Icon"
hubIcon.Size = UDim2.new(0, 40, 0, 40)
hubIcon.Position = UDim2.new(0, 20, 0, 20)
hubIcon.BackgroundTransparency = 1
hubIcon.Image = "rbxassetid://7733965388"
hubIcon.ImageColor3 = colors.primary
hubIcon.Parent = sidebarHeader

local hubTitle = Instance.new("TextLabel")
hubTitle.Name = "Title"
hubTitle.Size = UDim2.new(1, -70, 0, 25)
hubTitle.Position = UDim2.new(0, 70, 0, 20)
hubTitle.BackgroundTransparency = 1
hubTitle.Text = "Hell Hub"
hubTitle.TextColor3 = colors.text
hubTitle.TextSize = 22
hubTitle.Font = Enum.Font.GothamBold
hubTitle.TextXAlignment = Enum.TextXAlignment.Left
hubTitle.Parent = sidebarHeader

local hubVersion = Instance.new("TextLabel")
hubVersion.Name = "Version"
hubVersion.Size = UDim2.new(1, -70, 0, 18)
hubVersion.Position = UDim2.new(0, 70, 0, 45)
hubVersion.BackgroundTransparency = 1
hubVersion.Text = "Premium v3.0"
hubVersion.TextColor3 = colors.textMuted
hubVersion.TextSize = 12
hubVersion.Font = Enum.Font.Gotham
hubVersion.TextXAlignment = Enum.TextXAlignment.Left
hubVersion.Parent = sidebarHeader

-- Navigation Buttons Container
local navContainer = Instance.new("Frame")
navContainer.Name = "NavContainer"
navContainer.Size = UDim2.new(1, -20, 1, -100)
navContainer.Position = UDim2.new(0, 10, 0, 90)
navContainer.BackgroundTransparency = 1
navContainer.Parent = sidebar

local navLayout = Instance.new("UIListLayout", navContainer)
navLayout.Padding = UDim.new(0, 8)
navLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Tab Data
local tabs = {
    {name = "Main", icon = "🏠", layout = 1},
    {name = "Combat", icon = "⚔️", layout = 2},
    {name = "Visuals", icon = "👁️", layout = 3},
    {name = "Config", icon = "⚙️", layout = 4},
    {name = "Misc", icon = "🛠️", layout = 5}
}

local tabButtons = {}
local tabContents = {}
local activeTab = "Main"

--// CONTENT AREA
local contentArea = Instance.new("Frame")
contentArea.Name = "ContentArea"
contentArea.Size = UDim2.new(1, -200, 1, 0)
contentArea.Position = UDim2.new(0, 200, 0, 0)
contentArea.BackgroundTransparency = 1
contentArea.Parent = mainFrame

-- Window Controls
local closeBtn = Instance.new("TextButton")
closeBtn.Name = "Close"
closeBtn.Size = UDim2.new(0, 36, 0, 36)
closeBtn.Position = UDim2.new(1, -46, 0, 10)
closeBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
closeBtn.Text = ""
closeBtn.Parent = contentArea

local closeCorner = Instance.new("UICorner", closeBtn)
closeCorner.CornerRadius = UDim.new(0, 10)

local closeIcon = Instance.new("ImageLabel")
closeIcon.Size = UDim2.new(0, 16, 0, 16)
closeIcon.Position = UDim2.new(0.5, -8, 0.5, -8)
closeIcon.BackgroundTransparency = 1
closeIcon.Image = "rbxassetid://7733717447"
closeIcon.ImageColor3 = colors.text
closeIcon.Parent = closeBtn

local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Name = "Minimize"
minimizeBtn.Size = UDim2.new(0, 36, 0, 36)
minimizeBtn.Position = UDim2.new(1, -88, 0, 10)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
minimizeBtn.Text = ""
minimizeBtn.Parent = contentArea

local minimizeCorner = Instance.new("UICorner", minimizeBtn)
minimizeCorner.CornerRadius = UDim.new(0, 10)

local minimizeIcon = Instance.new("ImageLabel")
minimizeIcon.Size = UDim2.new(0, 16, 0, 16)
minimizeIcon.Position = UDim2.new(0.5, -8, 0.5, -8)
minimizeIcon.BackgroundTransparency = 1
minimizeIcon.Image = "rbxassetid://7733798692"
minimizeIcon.ImageColor3 = colors.text
minimizeIcon.Parent = minimizeBtn

--// CREATE NAV BUTTON FUNCTION
local function createNavButton(tabData)
    local btn = Instance.new("TextButton")
    btn.Name = tabData.name .. "Btn"
    btn.Size = UDim2.new(1, 0, 0, 44)
    btn.BackgroundColor3 = tabData.name == activeTab and colors.surface or Color3.fromRGB(0, 0, 0, 0)
    btn.BackgroundTransparency = tabData.name == activeTab and 0 or 1
    btn.Text = ""
    btn.LayoutOrder = tabData.layout
    btn.Parent = navContainer
    
    local btnCorner = Instance.new("UICorner", btn)
    btnCorner.CornerRadius = UDim.new(0, 12)
    
    -- Icon
    local icon = Instance.new("TextLabel")
    icon.Name = "Icon"
    icon.Size = UDim2.new(0, 30, 0, 30)
    icon.Position = UDim2.new(0, 12, 0.5, -15)
    icon.BackgroundTransparency = 1
    icon.Text = tabData.icon
    icon.TextSize = 20
    icon.Font = Enum.Font.Gotham
    icon.Parent = btn
    
    -- Label
    local label = Instance.new("TextLabel")
    label.Name = "Label"
    label.Size = UDim2.new(1, -60, 1, 0)
    label.Position = UDim2.new(0, 50, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = tabData.name
    label.TextColor3 = tabData.name == activeTab and colors.text or colors.textMuted
    label.TextSize = 15
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = btn
    
    -- Hover effect
    btn.MouseEnter:Connect(function()
        if activeTab ~= tabData.name then
            tween(btn, {BackgroundColor3 = colors.surface, BackgroundTransparency = 0.5}, 0.2)
        end
    end)
    
    btn.MouseLeave:Connect(function()
        if activeTab ~= tabData.name then
            tween(btn, {BackgroundTransparency = 1}, 0.2)
        end
    end)
    
    -- Click handler
    btn.MouseButton1Click:Connect(function()
        if activeTab == tabData.name then return end
        
        -- Update previous tab
        for name, button in pairs(tabButtons) do
            tween(button, {BackgroundColor3 = Color3.fromRGB(0, 0, 0, 0), BackgroundTransparency = 1}, 0.2)
            button.Label.TextColor3 = colors.textMuted
        end
        
        -- Hide all contents
        for name, content in pairs(tabContents) do
            content.Visible = false
        end
        
        -- Activate new tab
        activeTab = tabData.name
        tween(btn, {BackgroundColor3 = colors.surface, BackgroundTransparency = 0}, 0.2)
        btn.Label.TextColor3 = colors.text
        
        -- Show content
        if tabContents[tabData.name] then
            tabContents[tabData.name].Visible = true
        end
    end)
    
    return btn
end

--// CREATE CONTENT FRAME FUNCTION
local function createContentFrame(name)
    local frame = Instance.new("ScrollingFrame")
    frame.Name = name .. "Content"
    frame.Size = UDim2.new(1, -20, 1, -70)
    frame.Position = UDim2.new(0, 10, 0, 60)
    frame.BackgroundTransparency = 1
    frame.BorderSizePixel = 0
    frame.ScrollBarThickness = 4
    frame.ScrollBarImageColor3 = colors.primary
    frame.Visible = name == activeTab
    frame.CanvasSize = UDim2.new(0, 0, 0, 0)
    frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
    frame.Parent = contentArea
    
    return frame
end

--// CREATE UI ELEMENTS

-- Section Header
local function createSection(parent, title, yPos)
    local section = Instance.new("Frame")
    section.Name = title .. "Section"
    section.Size = UDim2.new(1, -10, 0, 30)
    section.Position = UDim2.new(0, 5, 0, yPos)
    section.BackgroundTransparency = 1
    section.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = title
    label.TextColor3 = colors.text
    label.TextSize = 16
    label.Font = Enum.Font.GothamBold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = section
    
    return yPos + 40
end

-- Modern Toggle Switch (like image)
local function createToggle(parent, text, yPos, callback)
    local container = Instance.new("Frame")
    container.Name = text .. "Toggle"
    container.Size = UDim2.new(1, -10, 0, 56)
    container.Position = UDim2.new(0, 5, 0, yPos)
    container.BackgroundColor3 = colors.surface
    container.BorderSizePixel = 0
    container.Parent = parent
    
    local containerCorner = Instance.new("UICorner", container)
    containerCorner.CornerRadius = UDim.new(0, 14)
    
    -- Label
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0, 200, 1, 0)
    label.Position = UDim2.new(0, 20, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = colors.text
    label.TextSize = 15
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container
    
    -- Toggle Background
    local toggleBg = Instance.new("Frame")
    toggleBg.Name = "ToggleBg"
    toggleBg.Size = UDim2.new(0, 52, 0, 28)
    toggleBg.Position = UDim2.new(1, -72, 0.5, -14)
    toggleBg.BackgroundColor3 = colors.toggleBg
    toggleBg.BorderSizePixel = 0
    toggleBg.Parent = container
    
    local toggleCorner = Instance.new("UICorner", toggleBg)
    toggleCorner.CornerRadius = UDim.new(1, 0)
    
    -- Toggle Knob (white circle)
    local knob = Instance.new("Frame")
    knob.Name = "Knob"
    knob.Size = UDim2.new(0, 24, 0, 24)
    knob.Position = UDim2.new(0, 2, 0.5, -12)
    knob.BackgroundColor3 = colors.white
    knob.BorderSizePixel = 0
    knob.Parent = toggleBg
    
    local knobCorner = Instance.new("UICorner", knob)
    knobCorner.CornerRadius = UDim.new(1, 0)
    
    -- Click area
    local clickArea = Instance.new("TextButton")
    clickArea.Size = UDim2.new(1, 0, 1, 0)
    clickArea.BackgroundTransparency = 1
    clickArea.Text = ""
    clickArea.Parent = container
    
    local enabled = false
    
    local function updateToggle()
        if enabled then
            tween(toggleBg, {BackgroundColor3 = colors.toggleOn}, 0.2)
            tween(knob, {Position = UDim2.new(0, 26, 0.5, -12)}, 0.2)
        else
            tween(toggleBg, {BackgroundColor3 = colors.toggleBg}, 0.2)
            tween(knob, {Position = UDim2.new(0, 2, 0.5, -12)}, 0.2)
        end
        if callback then callback(enabled) end
    end
    
    clickArea.MouseButton1Click:Connect(function()
        enabled = not enabled
        updateToggle()
    end)
    
    return yPos + 66, function() return enabled end, container
end

-- Modern Slider (blue track like image)
local function createSlider(parent, title, yPos, min, max, default, callback)
    local container = Instance.new("Frame")
    container.Name = title .. "Slider"
    container.Size = UDim2.new(1, -10, 0, 80)
    container.Position = UDim2.new(0, 5, 0, yPos)
    container.BackgroundColor3 = colors.surface
    container.BorderSizePixel = 0
    container.Parent = parent
    
    local containerCorner = Instance.new("UICorner", container)
    containerCorner.CornerRadius = UDim.new(0, 14)
    
    -- Title
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 0, 25)
    label.Position = UDim2.new(0, 20, 0, 15)
    label.BackgroundTransparency = 1
    label.Text = title
    label.TextColor3 = colors.text
    label.TextSize = 15
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container
    
    -- Value display
    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0, 60, 0, 25)
    valueLabel.Position = UDim2.new(1, -80, 0, 15)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = tostring(default)
    valueLabel.TextColor3 = colors.textMuted
    label.TextSize = 14
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.TextXAlignment = Enum.TextXAlignment.Right
    valueLabel.Parent = container
    
    -- Slider track background
    local trackBg = Instance.new("Frame")
    trackBg.Name = "TrackBg"
    trackBg.Size = UDim2.new(1, -40, 0, 8)
    trackBg.Position = UDim2.new(0, 20, 0, 50)
    trackBg.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
    trackBg.BorderSizePixel = 0
    trackBg.Parent = container
    
    local trackCorner = Instance.new("UICorner", trackBg)
    trackCorner.CornerRadius = UDim.new(1, 0)
    
    -- Slider fill (blue)
    local fill = Instance.new("Frame")
    fill.Name = "Fill"
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = colors.accent
    fill.BorderSizePixel = 0
    fill.Parent = trackBg
    
    local fillCorner = Instance.new("UICorner", fill)
    fillCorner.CornerRadius = UDim.new(1, 0)
    
    -- Slider knob (white circle)
    local knob = Instance.new("Frame")
    knob.Name = "Knob"
    knob.Size = UDim2.new(0, 20, 0, 20)
    knob.Position = UDim2.new((default - min) / (max - min), -10, 0.5, -10)
    knob.BackgroundColor3 = colors.white
    knob.BorderSizePixel = 0
    knob.Parent = trackBg
    
    local knobCorner = Instance.new("UICorner", knob)
    knobCorner.CornerRadius = UDim.new(1, 0)
    
    -- Shadow for knob
    local knobShadow = Instance.new("ImageLabel")
    knobShadow.Size = UDim2.new(1.5, 0, 1.5, 0)
    knobShadow.Position = UDim2.new(-0.25, 0, -0.25, 0)
    knobShadow.BackgroundTransparency = 1
    knobShadow.Image = "rbxassetid://13131898739" -- shadow
    knobShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
    knobShadow.ImageTransparency = 0.7
    knobShadow.Parent = knob
    
    -- Interaction
    local dragging = false
    
    local function updateSlider(inputX)
        local trackStart = trackBg.AbsolutePosition.X
        local trackWidth = trackBg.AbsoluteSize.X
        local relativeX = math.clamp((inputX - trackStart) / trackWidth, 0, 1)
        
        fill.Size = UDim2.new(relativeX, 0, 1, 0)
        knob.Position = UDim2.new(relativeX, -10, 0.5, -10)
        
        local value = math.floor(min + (max - min) * relativeX)
        valueLabel.Text = tostring(value)
        
        if callback then callback(value) end
        return value
    end
    
    trackBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            updateSlider(input.Position.X)
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input.Position.X)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
    
    return yPos + 90
end

-- Button (like Save/Reset in image)
local function createButton(parent, text, yPos, icon, isPrimary)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.48, -5, 0, 48)
    btn.Position = UDim2.new(isPrimary and 0 or 0.52, 5, 0, yPos)
    btn.BackgroundColor3 = isPrimary and colors.primary or colors.surface
    btn.Text = ""
    btn.Parent = parent
    
    local btnCorner = Instance.new("UICorner", btn)
    btnCorner.CornerRadius = UDim.new(0, 14)
    
    -- Icon
    if icon then
        local iconLabel = Instance.new("ImageLabel")
        iconLabel.Size = UDim2.new(0, 20, 0, 20)
        iconLabel.Position = UDim2.new(0, 16, 0.5, -10)
        iconLabel.BackgroundTransparency = 1
        iconLabel.Image = icon
        iconLabel.ImageColor3 = isPrimary and colors.white or colors.text
        iconLabel.Parent = btn
    end
    
    -- Text
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, icon and -50 or -20, 1, 0)
    label.Position = UDim2.new(0, icon and 45 or 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = isPrimary and colors.white or colors.text
    label.TextSize = 15
    label.Font = Enum.Font.GothamBold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = btn
    
    -- Hover
    btn.MouseEnter:Connect(function()
        tween(btn, {BackgroundColor3 = isPrimary and colors.primaryHover or colors.surfaceHover}, 0.2)
    end)
    
    btn.MouseLeave:Connect(function()
        tween(btn, {BackgroundColor3 = isPrimary and colors.primary or colors.surface}, 0.2)
    end)
    
    return yPos + 58
end

--// BUILD TABS
for _, tabData in ipairs(tabs) do
    tabButtons[tabData.name] = createNavButton(tabData)
    tabContents[tabData.name] = createContentFrame(tabData.name)
end

-- MAIN TAB
local mainY = 10
mainY = createSection(tabContents.Main, "⚔️ Combat Features", mainY)
mainY = createToggle(tabContents.Main, "Instant Kill", mainY)
mainY = createToggle(tabContents.Main, "Auto Farm", mainY)
mainY = createToggle(tabContents.Main, "God Mode", mainY)

mainY = mainY + 20
mainY = createSection(tabContents.Main, "🌍 World", mainY)
mainY = createToggle(tabContents.Main, "Spawn Boss", mainY)

-- COMBAT TAB
local combatY = 10
combatY = createSection(tabContents.Combat, "🎯 Aimbot", combatY)
combatY = createToggle(tabContents.Combat, "Enable Aimbot", combatY)
combatY = createToggle(tabContents.Combat, "Silent Aim", combatY)
combatY = createToggle(tabContents.Combat, "Team Check", combatY)

combatY = combatY + 20
combatY = createSection(tabContents.Combat, "💥 Weapon Mods", combatY)
combatY = createToggle(tabContents.Combat, "Rapid Fire", combatY)
combatY = createToggle(tabContents.Combat, "No Recoil", combatY)
combatY = createToggle(tabContents.Combat, "Infinite Ammo", combatY)

-- VISUALS TAB
local visualY = 10
visualY = createSection(tabContents.Visuals, "👁️ ESP", visualY)
visualY = createToggle(tabContents.Visuals, "Player ESP", visualY)
visualY = createToggle(tabContents.Visuals, "Item ESP", visualY)
visualY = createToggle(tabContents.Visuals, "Chest ESP", visualY)

visualY = visualY + 20
visualY = createSection(tabContents.Visuals, "✨ Visual Mods", visualY)
visualY = createToggle(tabContents.Visuals, "Fullbright", visualY)
visualY = createToggle(tabContents.Visuals, "No Fog", visualY)

-- CONFIG TAB
local configY = 10
configY = createSection(tabContents.Config, "⚙️ Configuration", configY)
configY = createButton(tabContents.Config, "Create Config", configY, "rbxassetid://7733964719", true)
configY = createButton(tabContents.Config, "Load Config", configY, "rbxassetid://7733965144", false)

configY = configY + 20
configY = createSection(tabContents.Config, "💾 Management", configY)
configY = createButton(tabContents.Config, "Save", configY, "rbxassetid://7733946813", true)
configY = createButton(tabContents.Config, "Reset", configY, "rbxassetid://7733954760", false)

-- MISC TAB (FUNCTIONAL)
local miscY = 10
miscY = createSection(tabContents.Misc, "🚀 Movement", miscY)

-- Fly Toggle (Functional)
local flyY, getFlyState = createToggle(tabContents.Misc, "Fly Mode", miscY, function(enabled)
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    if enabled then
        local bg = Instance.new("BodyGyro")
        bg.Name = "HH_FlyGyro"
        bg.P = 9e4
        bg.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
        bg.CFrame = hrp.CFrame
        bg.Parent = hrp
        
        local bv = Instance.new("BodyVelocity")
        bv.Name = "HH_FlyVel"
        bv.Velocity = Vector3.new(0, 0, 0)
        bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        bv.Parent = hrp
    else
        for _, v in ipairs(hrp:GetChildren()) do
            if v.Name == "HH_FlyGyro" or v.Name == "HH_FlyVel" then
                v:Destroy()
            end
        end
    end
end)

miscY = miscY + 90

-- WalkSpeed Slider (Functional)
miscY = createSlider(tabContents.Misc, "WalkSpeed", miscY, 16, 500, 16, function(value)
    local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.WalkSpeed = value end
end)

-- JumpPower Slider
miscY = createSlider(tabContents.Misc, "JumpPower", miscY, 50, 500, 50, function(value)
    local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.JumpPower = value end
end)

-- Infinite Jump
miscY = createToggle(tabContents.Misc, "Infinite Jump", miscY)

-- Fly Loop
RunService.RenderStepped:Connect(function()
    if getFlyState and getFlyState() then
        local char = player.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local cam = workspace.CurrentCamera
            if hrp and cam then
                local bg = hrp:FindFirstChild("HH_FlyGyro")
                local bv = hrp:FindFirstChild("HH_FlyVel")
                
                if bg and bv then
                    bg.CFrame = cam.CFrame
                    
                    local move = Vector3.new(0, 0, 0)
                    if UserInputService:IsKeyDown(Enum.KeyCode.W) then move = move + cam.CFrame.LookVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.S) then move = move - cam.CFrame.LookVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.A) then move = move - cam.CFrame.RightVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.D) then move = move + cam.CFrame.RightVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move = move + Vector3.new(0, 1, 0) end
                    if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then move = move - Vector3.new(0, 1, 0) end
                    
                    bv.Velocity = move.Magnitude > 0 and move.Unit * 60 or Vector3.new(0, 0, 0)
                end
            end
        end
    end
end)

--// LOADING SEQUENCE
spawn(function()
    wait(0.5)
    
    local steps = {
        {text = "Bypassing security...", progress = 0.15, delay = 0.4},
        {text = "Loading modules...", progress = 0.35, delay = 0.5},
        {text = "Initializing UI...", progress = 0.6, delay = 0.4},
        {text = "Connecting...", progress = 0.85, delay = 0.5},
        {text = "Ready!", progress = 1, delay = 0.3}
    }
    
    for _, step in ipairs(steps) do
        loadingStatus.Text = step.text
        tween(progressFill, {Size = UDim2.new(step.progress, 0, 1, 0)}, step.delay)
        wait(step.delay)
    end
    
    -- Fade out loading
    tween(loadingOverlay, {BackgroundTransparency = 1}, 0.4)
    tween(loadingIcon, {ImageTransparency = 1}, 0.4)
    tween(loadingTitle, {TextTransparency = 1}, 0.4)
    tween(loadingSubtitle, {TextTransparency = 1}, 0.4)
    tween(loadingStatus, {TextTransparency = 1}, 0.4)
    tween(progressContainer, {BackgroundTransparency = 1}, 0.4)
    tween(progressFill, {BackgroundTransparency = 1}, 0.4)
    
    wait(0.5)
    loadingOverlay.Visible = false
    
    -- Show main UI with animation
    mainFrame.Visible = true
    mainFrame.Size = UDim2.new(0, 0, 0, 0)
    tween(mainFrame, {Size = UDim2.new(0, 700, 0, 450)}, 0.5, Enum.EasingStyle.Back)
end)

--// WINDOW CONTROLS
local guiVisible = true

local function toggleGui()
    guiVisible = not guiVisible
    if guiVisible then
        mainFrame.Visible = true
        tween(mainFrame, {Size = UDim2.new(0, 700, 0, 450)}, 0.3, Enum.EasingStyle.Back)
    else
        tween(mainFrame, {Size = UDim2.new(0, 0, 0, 0)}, 0.3, Enum.EasingStyle.Quad).Completed:Connect(function()
            mainFrame.Visible = false
        end)
    end
end

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

minimizeBtn.MouseButton1Click:Connect(function()
    toggleGui()
end)

-- Keybind (RightShift)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.RightShift then
        toggleGui()
    end
end)

--// DRAGGING
local dragging = false
local dragStart
local startPos

TopBar = sidebarHeader

sidebarHeader.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

print("✓ Hell Hub Premium Loaded")
print("Press RightShift to toggle")